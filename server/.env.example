# Pickleball Booking System - Server Configuration
# IMPORTANT: Set all REQUIRED fields before starting server

# Server Port
PORT=5000

# MySQL / XAMPP Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=pickleball_db

# Frontend CORS - Set to production URL in deployment
FRONTEND_URL=http://localhost:5173

# Environment
NODE_ENV=development

# JWT Security - REQUIRED - Change this to a strong random string in production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=8a4b14eb67a2921c0b4f2346d7c7c361412a874c156508161cb089c30e1594fc
JWT_EXPIRATION=7d

